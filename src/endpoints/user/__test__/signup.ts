/**
 * - Test signup with good data
 *   - Test that a user is created
 *   - Test that a client is created
 *   - Test that a token is created
 *   - Test the resulting user, client, and token
 *   - Test that a root block is created for the user
 * - Test that another user can't signup with the same email
 * - Test that when client is passed, it is used not a new one created
 * - Test signup with bad data fails
 */
