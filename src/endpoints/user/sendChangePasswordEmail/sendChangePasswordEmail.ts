import { ISendChangePasswordEmailContext } from "./types";

async function sendChangePasswordEmail(
  context: ISendChangePasswordEmailContext
): Promise<any> {
  // return context.sendChangePasswordEmail(emailAddress, query, expiration);
}

export default sendChangePasswordEmail;
