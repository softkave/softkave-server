const types = ``;

const endpoint = `
changeOptionPosition (
    customId: String!, 
    prevOptionId: String) : SingleCustomOptionResponse
`;

export const changeOptionPositionEndpointSchema = {
    types,
    endpoint,
};
